<h1>Time to work out!</h1>
<h2>Please add your exercise here:</h2>
<form id="entryForm">
  <label for="name">Workout Name:</label>
  <input type="text" name="name" id="name">
  <label for="reps">Number of Reps:</label>
  <input type="number" name="reps" id="reps">
  <label for="weight">Weight:</label>
  <input type="number" name="weight" id="weight">
  <label for="unit">Units:</label>
  <input type="checkbox" name="unit" id="unit"><br>
  <label for="date">Date in Month-Day-Year: </label>
  <input type="date" name="date" id="date"><br>
  <input type="submit" name="exerciseButton" value="Add Exercise" id="exerciseButton">
</form>

<table id="workoutTable">
    <tr>
        <th>Name</th>
        <th>Reps</th>
        <th>Weight</th>
        <th>Unit</th>
        <th>Date</th>
        <th>Edit</th>
        <th>Delete</th>
    </tr>
    
    {{#if data}}
        {{#each data}}
            <tr>
                <td>{{this.name}}</td>
                <td>{{this.reps}}</td>
                <td>{{this.weight}}</td>
                <td>{{this.unit}}</td>
                <td>{{this.date}}</td>
                <td>{{this.pounds}}</td>
                <td><a href="/editForm?id={{this.id}}"><input type="button" value="Edit"></a></td>
                <td>
                    <input type="button" name="delete" value="Delete" onClick="removeData('dataTable',{{this.id}})">
                    <input type="hidden" id="delete{{this.id}}">
                </td>
            </tr>
        {{/each}}
    {{/if}}
</table>

<script src="./script.js"></script>